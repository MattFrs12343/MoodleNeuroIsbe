# CHANGELOG — Plataforma Educativa

> Registrar avances por fase (Spec-Driven Development). Actualizar con cada tarjeta SDD cerrada.

## Fase 0 — Variables fijas
- [ ] `specs/00-variables-fijas.md` completado (dominio, credenciales).

## Fase 1 — Infraestructura (SDD-INF-*)
- [x] SDD-INF-01..07 verificadas en hosting real (`portal.examenes-neuro.com`), 2026-08-30.
  Detalle y desajustes reales vs. plantilla genérica: `specs/01-fase-infra.md`.

## Fase 2 — Configuración (SDD-CFG-*)
- [x] SDD-CFG-01..06 verificadas en hosting real, 2026-08-30. Fase 2 completa (banner de
  cookies diferido a Fase 5, es trabajo de tema).

## Fase 3 — Estructura (SDD-EST-*)
- [x] SDD-EST-01, 02 verificadas en hosting real, 2026-08-30.
- [x] SDD-EST-04: convención fijada (uso real pendiente de Fase 4).
- [ ] SDD-EST-03: bloqueada, falta número/orden real de módulos del Dr.

## Fase 4 — Carga (SDD-CAR-*)
- [ ] (pendiente)

## Fase 5 — Tema/Portada (SDD-TEM-*)
- [x] SDD-TEM-01, 02 completadas 2026-08-31 (tema activo, hero con imagen real).
- [~] SDD-TEM-04: logo/favicon hechos, footer/banner de cookies pendiente.
- [ ] SDD-TEM-03: bloqueada, sin cursos reales que listar en la portada.

## Fase 6 — QA/Producción (SDD-QA-*)
- [x] SDD-QA-05: backups automáticos, adelantada 2026-08-31 (no depende de contenido/tema).
- [x] SDD-QA-03: endurecimiento final, adelantada 2026-08-31 (0 alertas críticas).
- [~] SDD-QA-01: matriz TC parcial 2026-08-31 (lo verificable sin contenido/tema).
- [ ] Resto de la fase (SDD-QA-02, 04, 06..09): pendiente, sigue el orden normal.

## Historial
| Fecha | Detalle |
|---|---|
| 2026-08-28 | Proyecto contratado: **Bs 6,000** (50% inicio + 50% entrega). Docs y SDD creados. |
| 2026-08-30 | Auditoría completa de `docs/`, `specs/`, `tasks/` y `src/`. Gaps completados: (1) creado `specs/plantillas/lista_ejemplo.csv` (faltaba, referenciado desde 3 archivos); (2) corregido bug en `src/local/importcontenido/cli/importar.php` (faltaba `require_once course/modlib.php`, `create_module()` habría fallado con fatal error); (3) corregido typo en `specs/04-fase-carga.md`; (4) creadas las 5 skills de `docs/14-mcps-skills.md` en `.claude/skills/` (T0.3); (5) creado `.mcp.json` (Playwright + MySQL, sin credenciales embebidas). Pendiente de decisión del desarrollador (no se improvisó): wiring de `local_importcontenido_render_modulos_grid()` (hoy no está conectada a ninguna página/filtro; v1 de `docs/06-portada-tema.md` opta por bloques nativos), assets de marca (`pix/logo.svg`/`favicon.ico`, bloqueados por `docs/13-info-necesaria.md` §5). Confirmado con el desarrollador: mantener la decisión v1 de portada (bloques nativos) tal cual, sin código nuevo por ahora. |
| 2026-08-30 | Git inicializado (`git init`), primer commit con el estado completo del repo (53 archivos) y push a `origin/main` (`https://github.com/MattFrs12343/MoodleNeuroIsbe.git`, repo remoto estaba vacío). Se agregó `.gitignore`. T0.4 pasa a EN CURSO (faltan tags por fase). |
| 2026-08-30 | Instalación de MCP + skills (T0.3): verificado que el entorno tiene Node/npm/npx pero NO Python/`uv`. Por eso se cambió el MCP de MySQL de `uvx mcp-server-mysql` (Python) a `@benborla29/mcp-server-mysql` (Node, vía `npx`) — mismo contrato de variables de entorno (`MYSQL_HOST/PORT/USER/PASS/DB`), decisión confirmada con el desarrollador. Ambos paquetes (`@playwright/mcp@0.0.79` y `@benborla29/mcp-server-mysql@2.0.9`) verificados como resolubles en el registro npm. Pendiente real (no de instalación): probar contra un Moodle/BD real, que no existe hasta Fase 1. `docs/14-mcps-skills.md` actualizado con la decisión y el checklist real. |
| 2026-08-30 | Decisión bloqueante resuelta (`docs/13-info-necesaria.md` #4): la licencia del curso comprado por el Dr. es de uso personal, un solo usuario. Esto cambia ADR-4 (`docs/00-maestro.md`): ya no hay auto-registro publico. Actualizados en cascada para eliminar la contradiccion: `docs/02-autenticacion.md` (self-registration OFF, cuenta unica creada por el admin, TC-A-01/DoD/riesgos reescritos), `specs/02-fase-config.md` (SDD-CFG-02 reescrita para deshabilitar self-registration en vez de activarlo con captcha; SDD-CFG-06 ajustada), `docs/08-legal-lgpd.md` (RF-L-08 y DoD marcados resueltos), `specs/00-variables-fijas.md` #7. Sigue pendiente el resto de `docs/13-info-necesaria.md` (dominio, credenciales, contenido, diseno, hosting); Fase 0 sigue bloqueada hasta completarlo. |
| 2026-08-30 | **Fase 1 (Infraestructura) completada y verificada sobre hosting real.** Dominio confirmado por el desarrollador: `portal.examenes-neuro.com`, acceso SSH a `matiasf6@sh006.hostgator.net`. Cuenta compartida con otros proyectos en produccion (`matiasf6_cuestionario_bd`, `matiasf6_pedidos_db`, varios subdominios de `examenes-neuro.com`) — todo el trabajo se aislo estrictamente a `portal.examenes-neuro.com` y `matiasf6_moodle_*`, sin tocar nada mas. Ejecutado por SSH via `uapi` (sin UI de cPanel): PHP 8.2 fijado solo para el subdominio; BD `matiasf6_moodle_db` + usuario creados (nota: `uapi Mysql setup_db_and_user` genera nombres aleatorios propios y se descarto tras probarlo y limpiarlo; se uso `create_database`/`create_user`/`set_privileges_on_database` con nombres explicitos); Moodle **4.5.13+ (405, stable)** descargado e instalado por CLI; idioma pt_br instalado automaticamente por el instalador. Tres desajustes reales encontrados y corregidos vs. la plantilla generica (detalle en `specs/01-fase-infra.md` y `specs/00-variables-fijas.md` #2): (1) el motor de BD real es **MySQL 8.0**, no MariaDB, por lo que se uso `--dbtype=mysqli`; (2) `$CFG->directorypermissions` se bajo de `02777` a `0700` (choque con umask del sistema, y ademas innecesario porque PHP corre como el propio usuario de la cuenta); (3) `$CFG->localrequestdir` tuvo que redirigirse a un directorio propio dentro de `dataroot`, porque el default de Moodle (`/tmp/requestdir`) esta "contaminado" por otra cuenta cPanel en el `/tmp` compartido del servidor (permisos `777` aparentes por `ls`, pero acceso real denegado). Cron programado cada minuto via `crontab` de usuario (esta cuenta no expone modulo `Cron` en `uapi`) y verificado sin errores. Sitio responde `200 OK` por HTTPS. Login temporal usado para el admin: email `dev@somoscdv.com` (del desarrollador, no del Dr. — cambiar cuando se confirme, ver `docs/13-info-necesaria.md` #2) y nombre de sitio "Plataforma de Estudos" (cambiar cuando el Dr. confirme el nombre final, `docs/13-info-necesaria.md` #1). Actualizados: `specs/01-fase-infra.md` (las 7 tarjetas SDD-INF cerradas con el detalle real), `docs/01-infraestructura.md` (DoD), `tasks/01-infra.md`, `tasks/00-estado-general.md` (punto de continuidad pasa a Fase 2), `docs/13-info-necesaria.md` (secciones 1, 2 parcial y 6 resueltas). |
| 2026-08-30 | **Fase 2 (Configuracion base) ejecutada, 5/6 tarjetas cerradas.** SDD-CFG-01: idioma pt_br forzado (la spec traia el flag `--value=` pero `admin/cli/cfg.php` de Moodle 4.5 usa `--set=`, corregido en la spec). SDD-CFG-02: se detecto que el instalador dejaba activo el plugin de auth `email`, el boton de invitado (`guestloginbutton=1`) y los metodos de inscripcion `guest`/`self`/`cohort` por defecto — todo esto contradecia el modo mono-usuario recien decidido, se deshabilito todo; sigue pendiente crear la cuenta real del Dr. (falta su nombre/email). SDD-CFG-03: ya estaba correcto sin cambios (rol por defecto `user`, sin asignaciones de teacher/manager). SDD-CFG-04: aplicado `protectusernames`, `passwordpolicy`, `preventexecpath`; al revisar el Informe de seguridad nativo de Moodle se encontraron **12 tipos de archivo interno publicamente descargables sin login** (`composer.json/lock`, `*.dist`, `admin/environment.xml`, `db/install.xml` de varios plugins, varios `README*`, `upgrade.txt`/`UPGRADING*.md`, y el contenido completo de cualquier carpeta `tests/` incluyendo `behat/` y `fixtures/`) — ninguno con secretos, pero exposicion de estructura interna innecesaria; corregido con reglas nuevas en `.htaccess` (bloqueo por patron de nombre/carpeta, sin tocar el bloque de PHP que gestiona cPanel). Verificado con `curl` archivo por archivo que todos devuelven ahora `403` (aceptado explicitamente como valido por el propio codigo de Moodle). Queda una nota abierta, no bloqueante: el badge de ese chequeo en la UI del Informe de seguridad sigue mostrando "Erro" pese a que cada ruta individual ya reporta 403 (posible cache/artefacto de render; se descarto seguir investigando para no perder tiempo en algo sin impacto real). SDD-CFG-06: verificado pt-BR sin strings en ingles en portada, login e informe de seguridad. **SDD-CFG-05 (textos legales LGPD) se dejo pendiente a proposito**: es redaccion de contenido legal real, no configuracion tecnica, y falta el dato de contacto para el aviso de privacidad (`docs/13-info-necesaria.md` #4); ademas cambia el mecanismo (Site Policy de Moodle en vez de checkbox de registro, porque ya no hay auto-registro). Actualizados: `specs/02-fase-config.md` (las 6 tarjetas con el detalle real), `docs/02-autenticacion.md` (DoD), `tasks/02-config.md`, `tasks/00-estado-general.md` (punto de continuidad pasa a Fase 3). |
| 2026-08-30 | **SDD-CFG-02 completada**: el desarrollador confirmo el nombre y correo real del Dr. (Dr. Juan Marcelo Cabello Merida, jmcabello_merida@hotmail.com). Se creo la unica cuenta de usuario (`dr_jcabello`, rol `user`, `auth=manual`) usando la API oficial `user_create_user()` de Moodle en un script temporal (se borro despues de usarlo) en vez del formulario web, para no depender de scraping de sesskey/CSRF. Se genero una clave temporal y se marco `auth_forcepasswordchange=1` para que el Dr. la cambie en su primer ingreso; verificado por `curl` que el login redirige correctamente a `login/change_password.php`. De paso se corrigio una nota incorrecta del CHANGELOG de Fase 1: el email `dev@somoscdv.com` del admin **no es un placeholder temporal a reemplazar por el del Dr.** — segun el modelo de roles (`docs/02-autenticacion.md`), el admin del sitio es el desarrollador de forma permanente; el Dr. tiene su propia cuenta separada con rol `user`. Actualizados: `specs/02-fase-config.md`, `docs/02-autenticacion.md`, `tasks/02-config.md`, `docs/13-info-necesaria.md` (#2), `specs/00-variables-fijas.md`. Con esto Fase 2 queda en 5/6; solo falta SDD-CFG-05 (textos legales), para la cual ya hay un dato de contacto viable (el email del Dr.) pero sigue pendiente redactar y revisar el contenido antes de publicarlo. |
| 2026-08-30 | **Fase 3 (Estructura) ejecutada hasta donde alcanzan los datos disponibles: 2.5/4.** SDD-EST-01: categorias `Plataforma` -> `Modulos` creadas via script con `core_course_category::create()`. SDD-EST-02: curso plantilla `mod00` creado con 6 secciones nombradas y metodo de inscripcion manual activo. **Se encontraron y corrigieron dos bugs reales**, ambos presentes tambien en `src/local/importcontenido/cli/importar.php` (el script que se usara en Fase 4, mismo patron de codigo): (1) `core_course_category::create_course()` no existe en Moodle 4.5 -- la funcion correcta es la global `create_course()` de `course/lib.php`; se descubrio porque el script de prueba fallo con "Call to undefined method" al intentarlo igual que lo hacia el plugin. (2) El valor de retorno de `course_create_sections_if_missing()` en esta version (basada en la nueva API `formatactions`) no sirve para actualizar el nombre de una seccion directamente -- el `$DB->update_record()` no fallaba pero tampoco persistia nada; hubo que releer el registro de `mdl_course_sections` despues de asegurar la seccion y recien ahi actualizar `name`. Ambos fixes se aplicaron tanto en el script de prueba como en el codigo real del plugin (`get_or_create_course()` y `ensure_section()`), evitando que la Fase 4 tropiece con lo mismo. De paso se corrigio una inconsistencia en `docs/03-modulos-cursos.md` (RF-M-06): asumia "auto-enrol" para los usuarios estandar, pero eso ya no existe porque `self`/`guest` enrolment se deshabilitaron en Fase 2 (SDD-CFG-02) -- el mecanismo real es inscribir manualmente al unico usuario (el Dr.) en cada curso real, trivial al ser una sola persona. **SDD-EST-03 (crear los cursos Modulo NN reales) queda bloqueada**: no se improvisaron nombres de modulos: falta que el Dr. entregue el numero real de modulos y su orden (`docs/13-info-necesaria.md` #3). Actualizados: `specs/03-fase-estructura.md`, `docs/03-modulos-cursos.md` (DoD + RF-M-06), `tasks/03-estructura.md`, `tasks/00-estado-general.md` (punto de continuidad: Fase 4 bloqueada por T3.3). |
| 2026-08-30 | **SDD-CFG-05 completada (Fase 2 queda 100%).** Con el contacto real del Dr. ya confirmado, se redactaron el Aviso de Privacidade y los Termos de Uso en pt-BR (base: `docs/08-legal-lgpd.md`) y se publicaron como **Site Policy** de Moodle (`admin/tool/policy`) via la API oficial `\tool_policy\api::form_policydoc_add()` + `make_current()`, en un script temporal borrado despues de usarlo (mismo patron que el resto de esta fase). Ambas politicas quedan con aceptacion **obligatoria** para cualquier usuario (incluida la cuenta del Dr., que aun no las acepto — se le pedira en su proximo login junto con el cambio de clave forzado). Esto reemplaza el mecanismo original de "checkbox en el registro" (ya no aplica sin auto-registro) y de paso cumple RF-L-06 (registro de consentimiento con fecha), que Moodle guarda automaticamente en `mdl_tool_policy_acceptances`. Verificado que el contenido se guardo completo (2150 y 1638 caracteres) y se renderiza con acentos correctos. **El banner de cookies queda pendiente y se movio a Fase 5** (`tasks/05-tema.md`, T5.4): se confirmo revisando `admin/settings/*.php` que Moodle 4.5 no trae un banner de cookies nativo, y agregarlo requiere CSS/JS del tema, no configuracion de backend. Actualizados: `specs/02-fase-config.md`, `docs/08-legal-lgpd.md` (DoD), `tasks/02-config.md`, `tasks/05-tema.md` (nueva sub-tarea), `tasks/00-estado-general.md` (Fase 2 marcada HECHA). |
| 2026-08-31 | **SDD-QA-05 (backups automaticos) adelantada fuera de orden**, a pedido del desarrollador de enfocarse en "backend de funcionamiento" mientras se esperan imagenes/contenido del Dr. Se creo una carpeta propia `~/moodle_backups/{db,dataroot}/` (separada de `~/backups/` y `~/nm-backups/`, que son de otros proyectos de la misma cuenta compartida — llenas de archivos `pedidos_*`/`predeploy-*`), con credenciales de mysqldump en un `.my.cnf` protegido (600) en vez de en el crontab en texto plano. Se instalaron los cron jobs (BD diaria 00:00 retencion 7d, dataroot semanal domingo 03:00 retencion 4 semanas) y se corrio un backup manual de prueba, verificado con exito: `bd_2026-08-31.sql.gz` (298 KB, 494 `CREATE TABLE` + 68 `INSERT INTO`, `gunzip -t` limpio) y `dataroot_2026-08-31.tar.gz` (4 MB, contenido verificado con `tar tzf`). `mysqldump` mostro un warning benigno sobre el privilegio PROCESS (normal en hosting compartido, no afecta el dump). **Hallazgo de infraestructura importante**: al revisar el `crontab` para agregar estas lineas, se encontro que la linea del cron de Moodle (Fase 1, SDD-INF-06) habia cambiado sola de `* * * * *` a `*/17 * * * *` de un dia para el otro, sin que nadie la tocara — el hosting reescribe automaticamente cron jobs de "cada minuto" a un intervalo mayor (throttling de cron, politica del proveedor en shared hosting). Se ajusto explicitamente a `*/5 * * * *` (razonable para un sitio de un solo usuario). Si se necesita cron cada 1 minuto de verdad, hay que pedirlo a soporte de HostGator. Actualizados: `specs/06-fase-qa.md` (SDD-QA-05), `specs/01-fase-infra.md` (SDD-INF-06, nota del hallazgo), `specs/00-variables-fijas.md` (CRON_EJEC_S y nueva variable BACKUPS), `docs/07-seguridad-backups.md` (DoD), `tasks/06-qa.md`, `tasks/00-estado-general.md`. |
| 2026-08-31 | **SDD-QA-03 (endurecimiento final) y SDD-QA-01 (matriz TC, parcial) adelantadas.** Endurecimiento: se encontro `lockoutthreshold=0` (bloqueo de cuenta tras intentos fallidos DESACTIVADO desde la instalacion) y se activo en `10`; `config.php` estaba en permisos `640` y se bajo a `400` (ni el propio dueno puede escribir sin `chmod` antes); se confirmo que no hay ClamAV disponible en este hosting compartido (sin acceso root para instalarlo), documentado como limitacion real, mitigado por la lista blanca de extensiones ya planeada para Fase 4. **Se resolvio de verdad, no solo se acepto, el "Erro" que quedaba abierto en el Informe de seguridad desde la Fase 2**: instanciando el chequeo `publicpaths` directo por CLI (sin pasar por ninguna cache de sesion) y revisando la tabla de detalle fila por fila (no solo el resumen, que deduplica y esconde items), se encontraron dos archivos reales que seguian publicos — `.github/FUNDING.yml` y `.stylelintrc` — nunca antes probados individualmente. Se corrigieron con una regla `.htaccess` que bloquea cualquier segmento de ruta que empiece con punto. **Resultado: 0 alertas criticas en el Informe de seguridad** (17 OK, 1 Info, 1 Aviso de baseline). De paso se corrigio la nota de la Fase 2 que atribuia esto a un posible artefacto de cache — no lo era. Matriz TC: se corrieron por `curl`/SQL reales (no lectura de codigo) las TC de `01-infraestructura` (4/5 verde), `02-autenticacion` (5/7), `03-modulos-cursos` (2/6) y `08-legal-lgpd` (1/6); las de `04-videos-embed`, `05-audio-documentos` y `06-portada-tema` quedan sin ejecutar, bloqueadas por completo por las Fases 4 y 5. Se reescribio TC-L-01 (ya no aplica "checkbox en registro", ahora es aceptacion obligatoria de Site Policy). Actualizados: `specs/06-fase-qa.md`, `specs/02-fase-config.md` (correccion de la nota de cache), `docs/01-infraestructura.md`, `docs/02-autenticacion.md`, `docs/03-modulos-cursos.md`, `docs/08-legal-lgpd.md` (matrices TC), `tasks/06-qa.md`, `tasks/00-estado-general.md`. |
| 2026-08-31 | **El desarrollador entrego 6 imagenes (`assets/img1..6.png`)** y se ejecuto la Fase 5 hasta donde alcanza (2/4). Identificacion visual: 2 de las 6 (`img1`, `img2`) tenian el fondo transparente "quemado" como cuadricula de ajedrez (RGB sin canal alfa, confirmado con `file`/PowerShell `System.Drawing`) -- descartadas con acuerdo del desarrollador. Usadas: `img3.png` (RGBA real) como logo/favicon, `img6.png` como fondo del hero (eleccion del desarrollador entre esa y `img5.png`). **Se encontraron y corrigieron DOS bugs reales de Moodle 4.5.13**, ninguno relacionado con el hosting (confirmado leyendo el codigo fuente del core linea por linea): (1) `$THEME->scss` como array de nombres de archivo (como traia la plantilla original desde su creacion) NO es valido -- debe ser un callback PHP. Con el array, Moodle nunca compilaba nuestro SCSS propio y servia CSS de boost sin ningun cambio, desde el dia 1 del tema -- nadie lo habia notado porque no habia imagenes reales contra las que comparar. Corregido con `lib.php` (`theme_plataforma_get_main_scss_content()`), mismo patron que `theme_classic`. (2) El logo/favicon configurados via `Site admin -> Appearance -> Logos` generan URLs rotas: `moodle_url::make_pluginfile_url()` concatena la revision del tema con el nombre de archivo sin separador, y `core_admin_pluginfile()` (`admin/lib.php`) no puede volver a separarlos -- siempre `404`. Se probo que separando los segmentos manualmente si funciona, aislando el problema al lado de generacion de URL de Moodle, no al servidor. Resuelto sin tocar core: favicon cae en el fallback nativo de `core_renderer::favicon()` (dejando `core_admin/favicon` vacio); el logo (que no tiene ese fallback) se resolvio con un renderer override propio del tema (`classes/output/core_renderer.php`, mecanismo oficial via `$THEME->rendererfactory`). Favicon `.ico` generado con ImageMagick (`convert`, disponible en el servidor). Verificado por `curl` que logo/favicon/hero-bg responden `200` y el CSS compilado ya trae `.hero`, `.modulo-card`, etc. **Falta confirmacion visual** (Playwright/captura): la extension de Chrome no estaba conectada en el momento de esta tarjeta. SDD-TEM-03 (portada con tarjetas) sigue bloqueada: no hay cursos reales que listar (Fase 3/4). Footer con enlaces legales y banner de cookies: pendientes. Actualizados: `specs/05-fase-tema.md` (SDD-TEM-01/02/04 con el detalle real), `docs/06-portada-tema.md` (DoD), `tasks/05-tema.md`, `docs/13-info-necesaria.md` (#5 resuelto), `tasks/00-estado-general.md`. |