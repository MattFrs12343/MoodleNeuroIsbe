# Datos y credenciales — nunca versionar
*.env
.env.*
**/config.php.local
moodledata/
*.sql
*.sql.gz

# SO / editores
.DS_Store
Thumbs.db
*.swp

# Node (por si algún MCP local instala dependencias aquí)
node_modules/

# Estado interno de la sesión del agente (no es contenido del proyecto)
.claude/scheduled_tasks.lock
